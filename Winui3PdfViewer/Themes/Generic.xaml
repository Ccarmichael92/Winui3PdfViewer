<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Winui3PdfViewer">

    <Style TargetType="local:PdfViewerControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:PdfViewerControl">
                    <Grid Background="{TemplateBinding Background}">

                        <!-- Scrollable content -->
                        <ScrollViewer x:Name="PART_ScrollViewer"
              HorizontalScrollBarVisibility="Auto"
              VerticalScrollBarVisibility="Auto"
              HorizontalScrollMode="Enabled"
              VerticalScrollMode="Enabled"
              ZoomMode="Disabled"
              HorizontalContentAlignment="Stretch"
              VerticalContentAlignment="Stretch">

                            <Grid x:Name="PART_ViewportRoot"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch">
                                <StackPanel x:Name="PART_PagePanel"
                                            Orientation="Vertical"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Top"
                                            Spacing="10"/>
                            </Grid>
                        </ScrollViewer>

                        <!-- Zoom overlay -->
                        <Grid HorizontalAlignment="Right"
                              VerticalAlignment="Top"
                              Margin="10">
                            <StackPanel Orientation="Horizontal"
                                        Background="#66000000"
                                        Padding="6"
                                        CornerRadius="4"
                                        Spacing="4">
                                <TextBlock x:Name="PART_PageOverlay"
                                           Text=""
                                           Foreground="White"
                                           FontWeight="Normal"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           FontSize="12"
                                           Margin="0 0 10 0"/>
                                <Button x:Name="PART_BtnZoomOut" Content="−" Width="32" Height="32"/>
                                <TextBlock x:Name="PART_ZoomOverlay"
                                           Text="100%"
                                           Foreground="White"
                                           FontWeight="Bold"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Margin="2 0 2 0"/>
                                <Button x:Name="PART_BtnZoomIn" Content="+" Width="32" Height="32"/>
                                <Button x:Name="PART_BtnFit" Content="Fit" Width="48" Height="32"/>
                                <Button x:Name="PART_Btn100" Content="100%" Width="55" Height="32"/>
                            </StackPanel>
                        </Grid>

                        <!-- Page navigation (single-page mode only) -->
                        <Button x:Name="PART_BtnPrevPage"
                                Content="◀"
                                Width="40" Height="100"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Visibility="Collapsed"
                                Margin="10"/>
                        <Button x:Name="PART_BtnNextPage"
                                Content="▶"
                                Width="40" Height="100"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Visibility="Collapsed"
                                Margin="10"/>

                        <Grid x:Name="PART_LoadingOverlay"
      Background="WhiteSmoke"
      Visibility="Collapsed"
      HorizontalAlignment="Stretch"
      VerticalAlignment="Stretch"
      >
                            <ProgressRing x:Name="PART_ProgressRing"
                  IsActive="False"
                  Width="80" Height="80"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"/>
                        </Grid>


                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>